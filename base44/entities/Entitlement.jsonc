{
  "name": "Entitlement",
  "type": "object",
  "properties": {
    "user_email": {
      "type": "string",
      "description": "Owner of this entitlement. Matched against the authenticated user's email."
    },
    "user_id": {
      "type": "string",
      "description": "Auth user id, when known. Secondary key: entitlements are matched by email OR id, so a record created by a job that only knew the id still resolves."
    },
    "plan": {
      "type": "string",
      "enum": [
        "free",
        "beta",
        "paid"
      ],
      "default": "free",
      "description": "beta = granted access (testers, invited therapists); paid = active Paddle or Stripe subscription"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "canceled",
        "past_due",
        "revoked"
      ],
      "default": "active"
    },
    "expires_at": {
      "type": "string",
      "format": "date-time",
      "description": "When access ends. NULL means lifetime \u2014 this is how founding testers are recorded."
    },
    "source": {
      "type": "string",
      "enum": [
        "beta_grant",
        "stripe",
        "paddle",
        "admin",
        "migration"
      ],
      "description": "Where this entitlement came from, for audit (Paddle is the primary launch provider; Stripe remains as fallback)"
    },
    "note": {
      "type": "string",
      "description": "Free-text reason, e.g. 'founding tester', 'invited therapist \u2014 Tenerife pilot'"
    },
    "granted_by": {
      "type": "string",
      "description": "Email of the admin who granted it, empty for paid providers"
    },
    "stripe_customer_id": {
      "type": "string"
    },
    "stripe_subscription_id": {
      "type": "string"
    },
    "paddle_customer_id": {
      "type": "string",
      "description": "Paddle customer ID (ctm_...) used to open the hosted customer portal"
    },
    "paddle_subscription_id": {
      "type": "string",
      "description": "Paddle subscription ID (sub_...) used for webhook upserts and portal deep links"
    },
    "price_id": {
      "type": "string",
      "description": "Current Paddle price ID"
    },
    "product_id": {
      "type": "string",
      "description": "Current Paddle product ID"
    },
    "scheduled_change": {
      "type": "string",
      "format": "date-time",
      "description": "When a scheduled cancellation or pause becomes effective"
    },
    "description": {
      "type": "string",
      "maxLength": 1000,
      "description": "Optional free-text description"
    }
  },
  "required": [
    "user_email",
    "plan"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}