{
  "name": "Session",
  "properties": {
    "ai_gateway_version": {
      "description": "Configured AI gateway/model routing revision used for this session",
      "type": "string"
    },
    "carry_over_context": {
      "description": "Short summary of the previous session's theme, injected into the opening greeting when the user chose to continue/deepen it instead of starting fresh",
      "type": "string"
    },
    "confidence_note": {
      "description": "Disclaimer note that the summary is automatic and should be checked by the user",
      "type": "string"
    },
    "continued_from_session_id": {
      "description": "If the user chose to deepen a previous completed session's theme, the id of that prior Session",
      "type": "string"
    },
    "current_step": {
      "default": 0,
      "type": "number"
    },
    "edge_signals": {
      "description": "Moments where resistance, an inner critic, shame, looping, or another process edge appeared",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "ended_at": {
      "format": "date-time",
      "type": "string"
    },
    "memory_excluded": {
      "default": false,
      "description": "User-controlled: if true, this session must not contribute to cross-session memory or AI retrieval",
      "type": "boolean"
    },
    "memory_excluded_at": {
      "format": "date-time",
      "type": "string"
    },
    "memory_exclusion_reason": {
      "maxLength": 1000,
      "type": "string"
    },
    "mode": {
      "description": "Session mode type",
      "enum": [
        "body",
        "dream",
        "conflict",
        "journaling"
      ],
      "type": "string"
    },
    "mode_id": {
      "description": "Mode identifier",
      "type": "string"
    },
    "next_step_suggestion": {
      "type": "string"
    },
    "primary_process": {
      "description": "What felt familiar or identity-consistent in this session",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "risk_flag": {
      "default": false,
      "type": "boolean"
    },
    "secondary_process": {
      "description": "What felt new, unfamiliar, or emergent in this session",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "signals": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "started_at": {
      "format": "date-time",
      "type": "string"
    },
    "status": {
      "default": "active",
      "enum": [
        "active",
        "completed",
        "abandoned"
      ],
      "type": "string"
    },
    "summary": {
      "description": "AI-generated session summary",
      "type": "string"
    },
    "system_prompt_version": {
      "description": "Talvira orchestration/system-prompt release used for this session",
      "type": "string"
    },
    "themes": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "user_id": {
      "description": "Authenticated Base44 user id that owns this session",
      "type": "string"
    },
    "continuation_requested": {
      "type": "boolean",
      "default": false
    },
    "continuation_started_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "mode"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  },
  "type": "object"
}